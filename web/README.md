# CPC Station Web

Vite + Vue 前端網站，提供：
- CPC 加油站地圖
- 最近站位（使用瀏覽器定位）
- 篩選（直營、自助、24H、廁所、服務）

## 開發

```bash
npm install
npm run dev
```

## 打包

```bash
npm run build
```

資料來源檔案使用 `public/data/all_stations.json`，由根目錄 `data/all_stations.json` 同步而來。
